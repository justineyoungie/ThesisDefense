<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/castle_bg"
    tools:context="com.thesis.thesisdefense.Activities.Story">

    <ImageView
        android:id="@+id/imageView"
        android:layout_width="577dp"
        android:layout_height="190dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_marginBottom="22dp"
        android:layout_marginEnd="36dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:srcCompat="@drawable/story_conversation" />

    <TextView
        android:id="@+id/text_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignEnd="@+id/imageView"
        android:layout_alignTop="@+id/imageView"
        android:layout_marginEnd="114dp"
        android:layout_marginTop="40dp"
        android:text="Name"
        android:textAlignment="center"
        android:textColor="#FFF"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@+id/imageView" />

    <com.thesis.thesisdefense.Activities.TypeWriter
        android:id="@+id/text_dialog"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignStart="@+id/imageView"
        android:layout_below="@+id/text_name"
        android:layout_marginStart="76dp"
        android:layout_marginTop="27dp"
        android:maxWidth="70dp"
        android:text="Dialog"
        android:textColor="#FFF" />

    <ImageView
        android:id="@+id/image_char"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/text_dialog"
        android:layout_marginBottom="8dp"
        android:layout_marginEnd="38dp"
        android:layout_marginTop="20dp"
        android:layout_toStartOf="@+id/text_name"
        android:visibility="invisible"
        app:srcCompat="@drawable/knight_portrait" />
</RelativeLayout>
